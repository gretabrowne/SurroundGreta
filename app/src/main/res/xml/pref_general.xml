<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="Input IP here"
        android:inputType="textCapWords"
        android:key="ipaddress"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:summary="App Restart Required to save changes"
        android:title="IP Address (Restart Required)" />
    <ListPreference
        android:defaultValue="-1"
        android:entries="@array/appIdslist"
        android:entryValues="@array/appIdslist"
        android:key="appid"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:summary="App Restart Required to save changes"
        android:title="@string/appID" />

</PreferenceScreen>
